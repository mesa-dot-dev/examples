{
  "name": "runloop-shell",
  "version": "0.1.0",
  "description": "Interactive shell over a Mesa cloud repo running in a Runloop devbox",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@mesadev/sdk": "0.46.0",
    "@runloop/api-client": "^1.22.1",
    "dotenv": "^17.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.3"
  }
}
