{
  "name": "change-management",
  "version": "0.1.0",
  "description": "Creates a Mesa repo with three changes (a, b, c), then lets you pick one to mount and explore via bash",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts"
  },
  "dependencies": {
    "@mesadev/sdk": "^0.20.0",
    "dotenv": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.8.0"
  }
}
